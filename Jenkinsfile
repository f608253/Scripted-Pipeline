node { 
      checkout scm   // to enable scm checkout which is not be default in scripted pipeline//
      stage ('Build') {
           echo 'Building....'
      }
      stage ('Test') {
           echo 'Testing..'
      }
      stage ('Deploy') {
           echo 'Deploying..'
      }
}
